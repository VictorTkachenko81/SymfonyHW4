{% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}
{{ form(form) }}
{#{{ form_start(form) }}#}
{#{{ form_widget(form) }}#}
    {#{% for score in form.scores %}#}
        {#{{ form_widget(score) }}#}
    {#{% endfor %}#}
{#{{ form_end(form) }}#}
